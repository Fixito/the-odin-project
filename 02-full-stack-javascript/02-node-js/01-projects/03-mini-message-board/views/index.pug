extends layout

block content
  h1= title
  p Welcome to #{title}
  a(href="/new") Send new message
  each message in messages
    div
      h2= message.user 
      p= message.text 
      p= message.added
